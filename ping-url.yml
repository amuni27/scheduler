name: Scheduled URL Ping

on:
  schedule:
    - cron: "*/5 * * * *"   # runs every 5 minutes
  workflow_dispatch:        # allows manual run

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit URL
        run: |
          curl -X GET https://go-zembil-frontend.onrender.com/
